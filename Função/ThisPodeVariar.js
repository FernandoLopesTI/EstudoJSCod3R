/**
 * Dentro do javaScript o This pode variar dentro de uma função que foi declarada como a palavra reservada function
 * dependendo de onde essa função for chamada.
 * No browser como por exemplo quando se criarmos uma função que compare o this com o window(que é o objeto global do browser) 
 * ele vai retornar true mais se atribuirmos essa mesma função para o clique do mouse na pagina ele vai retorna false pq o conceito de this mudou de local
 * 
 */